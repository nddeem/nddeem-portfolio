import { Award } from 'lucide-react';import SectionHeading from './SectionHeading';import Reveal from './Reveal';import { certificates } from '../data/certificates';
export default function Certificates(){return <section id="certificates" className="section certificates"><div className="section-index">04</div><SectionHeading eyebrow="Certification" title={<>Skills backed by<br/><span>focused learning.</span></>} copy="Professional training that strengthens my development fundamentals."/><div className="certificate-grid">{certificates.map((c,i)=><Reveal className="certificate-card glass-card" key={c.id} delay={i*.08}><div className="certificate-image"><Award/></div><div className="certificate-copy"><h3>{c.title}</h3><p className="certificate-org">{c.organization}</p><p className="certificate-description">{c.description}</p></div></Reveal>)}</div></section>}

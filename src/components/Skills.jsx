import SectionHeading from './SectionHeading';import Reveal from './Reveal';import { skillGroups } from '../data/skills';
export default function Skills(){return <section id="skills" className="section skills"><div className="section-index">02</div><SectionHeading eyebrow="Capabilities" title={<>Tools I use to turn<br/><span>ideas into interfaces.</span></>} copy="A growing toolkit for designing, building, and shipping thoughtful digital experiences."/><div className="skill-grid">{skillGroups.map((group,i)=><Reveal className="skill-card glass-card" delay={i*.07} key={group.title}><span className="card-number">0{i+1}</span><p className="mini-label">{group.eyebrow}</p><h3>{group.title}</h3><div className="skill-list">{group.skills.map(({name,icon:Icon})=><div className="skill-pill" key={name}><Icon/><span>{name}</span></div>)}</div></Reveal>)}</div></section>}
